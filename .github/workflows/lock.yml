---
name: lock

"on":
  schedule:
    - cron: "0 0 * * *"

permissions:
  actions: write
  issues: write
  pull-requests: write

concurrency:
  group: lock

jobs:
  execute:
    runs-on: ubuntu-latest

    steps:
      - uses: dessant/lock-threads@v3

...
